# Cortex AI Backend

REST API backend для Cortex AI - системы генерации изображений с AI.

## 🚀 Быстрый запуск

### Windows (автоматически):
```bash
setup.bat           # 1. Установка зависимостей
setup_database.bat  # 2. Настройка MySQL (создание БД и таблиц)
start_server.bat    # 3. Запуск сервера
```

### Windows (вручную):
```bash
# 1. Создать виртуальное окружение и установить зависимости
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt

# 2. Настроить .env файл (скопировать .env.example и указать пароль MySQL)
copy .env.example .env

# 3. Создать БД и применить схему
mysql -u root -p -e "CREATE DATABASE cortex_ai CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
mysql -u root -p cortex_ai < schema.sql

# 4. Запустить сервер
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Linux/Mac:
```bash
# 1. Создать виртуальное окружение и установить зависимости
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 2. Настроить .env (скопировать .env.example и указать пароль MySQL)
cp .env.example .env

# 3. Создать БД и применить схему
mysql -u root -p -e "CREATE DATABASE cortex_ai CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
mysql -u root -p cortex_ai < schema.sql

# 4. Запустить сервер
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**После запуска:**
- 📚 API документация: http://localhost:8000/api/docs
- 🔄 Альтернативная документация: http://localhost:8000/api/redoc

---

## ⚙️ Требования
- Python 3.10+
- MySQL 8.0+

## 📁 Структура проекта

```
back/
├── app/
│   ├── api/
│   │   ├── deps.py          # Зависимости (авторизация)
│   │   └── v1/              # API endpoints v1
│   │       ├── auth.py      # Аутентификация
│   │       ├── user.py      # Профиль пользователя
│   │       ├── subscriptions.py  # Подписки
│   │       ├── tokens.py    # Баланс токенов
│   │       ├── generate.py  # Генерация изображений
│   │       ├── gallery.py   # Галерея
│   │       └── stats.py     # Статистика
│   ├── core/
│   │   ├── config.py        # Настройки из .env
│   │   ├── database.py      # Подключение к БД
│   │   └── security.py      # JWT, хеширование паролей
│   ├── models/              # SQLAlchemy модели
│   ├── schemas/             # Pydantic схемы
│   ├── services/            # Бизнес-логика
│   └── main.py              # Точка входа FastAPI
├── alembic/                 # Миграции БД
├── .env                     # Настройки (НЕ коммитить!)
├── .env.example             # Шаблон настроек
├── requirements.txt         # Зависимости Python
└── schema.sql              # Схема базы данных
```

## 🔑 Основные эндпоинты

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/verify-email` - Подтверждение email
- `POST /api/auth/forgot-password` - Восстановление пароля
- `POST /api/auth/reset-password` - Сброс пароля
- `POST /api/auth/refresh-token` - Обновление токена

### Профиль (требуется авторизация)
- `GET /api/user/profile` - Получить профиль
- `PATCH /api/user/profile` - Обновить профиль
- `POST /api/user/avatar` - Загрузить аватар
- `PATCH /api/user/password` - Сменить пароль

### Подписки
- `GET /api/subscriptions/plans` - Тарифные планы
- `GET /api/subscriptions/current` - Текущая подписка
- `POST /api/subscriptions/subscribe` - Оформить подписку
- `POST /api/subscriptions/cancel` - Отменить подписку

### Генерация изображений
- `POST /api/generate/image` - Создать генерацию
- `GET /api/generate/status/{id}` - Статус генерации
- `GET /api/generate/history` - История генераций
- `DELETE /api/generate/{id}` - Удалить генерацию

### Галерея
- `GET /api/gallery` - Список изображений
- `GET /api/gallery/favorites` - Избранные
- `POST /api/gallery/{id}/favorite` - Добавить в избранное
- `DELETE /api/gallery/{id}` - Удалить изображение
- `GET /api/gallery/search` - Поиск

### Токены и статистика
- `GET /api/tokens/balance` - Баланс токенов
- `GET /api/tokens/history` - История использования
- `GET /api/stats/user` - Статистика пользователя

## 🛠️ Технологии

- **FastAPI** - веб-фреймворк
- **SQLAlchemy 2.0** - ORM (асинхронный)
- **Alembic** - миграции БД
- **MySQL 8.0** - база данных
- **JWT** - аутентификация
- **Bcrypt** - хеширование паролей
- **Pydantic** - валидация данных

## 📦 Зависимости

Основные пакеты (см. `requirements.txt`):
- fastapi==0.104.1
- uvicorn==0.24.0
- sqlalchemy==2.0.45
- alembic==1.17.2
- aiomysql==0.2.0
- python-jose==3.3.0
- bcrypt==5.0.0
- pydantic-settings==2.1.0

## 🔒 Безопасность

### Важно для production:
1. Измените `SECRET_KEY` в `.env` на случайный
2. Установите `DEBUG=False`
3. Настройте CORS для вашего фронтенда
4. Используйте HTTPS
5. Настройте реальный SMTP для отправки email
6. Добавьте rate limiting
7. Настройте логирование

### .env файл
**НИКОГДА не коммитьте .env в Git!** 
Файл содержит чувствительные данные (пароли, секретные ключи).

## 🐛 Возможные проблемы

### Ошибка подключения к MySQL
```
ОШИБКА: Can't connect to MySQL server
РЕШЕНИЕ: Проверьте пароль в .env, убедитесь что MySQL запущен
```

### Ошибка импорта pydantic_settings
```
ОШИБКА: No module named 'pydantic_settings'
РЕШЕНИЕ: pip install pydantic-settings
```

### Ошибка bcrypt
```
ОШИБКА: ValueError: password cannot be longer than 72 bytes
РЕШЕНИЕ: Уже исправлено в core/security.py (используется bcrypt напрямую)
```

## 📝 Разработка

### Создание новой миграции
```bash
alembic revision --autogenerate -m "description"
```

### Применение миграций
```bash
alembic upgrade head
```

### Откат миграции
```bash
alembic downgrade -1
```

## 🤝 Тарифные планы

- **Sirdar** (Free) - 1000 токенов на 3 дня
- **Expert** (1500₽/мес) - 24000 токенов
- **Lord** (12000₽/год) - безлимит токенов

## 📄 Лицензия

Проприетарная лицензия - все права защищены.

## 👨‍💻 Автор

Cortex AI Team
